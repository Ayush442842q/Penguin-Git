name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript, rust]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Initialize CodeQL
        uses: github/codeql-action/init@eb3cef721752a3421fe144a742539281bdf4df1a # v3
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@eb3cef721752a3421fe144a742539281bdf4df1a # v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@eb3cef721752a3421fe144a742539281bdf4df1a # v3
        with:
          category: "/language:${{ matrix.language }}"
